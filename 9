local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

local AimlockEnabled = false
local CurrentTarget = nil
local AimPart = "HumanoidRootPart"
local MaxDistance = 300

-- 1. TẠO GUI 
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "HiruzAimlockV14"
ScreenGui.ResetOnSpawn = false

local success = pcall(function()
    ScreenGui.Parent = (gethui and gethui()) or game:GetService("CoreGui")
end)
if not success then
    ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
end

-- 2. NÚT BẤM (CLONE 100% TSB - 79x79, KHÔNG VIỀN, NỀN ĐEN TRONG SUỐT)
local ToggleButton = Instance.new("TextButton", ScreenGui)
ToggleButton.Size = UDim2.new(0, 79, 0, 79) -- Kích thước chuẩn ông yêu cầu
ToggleButton.Position = UDim2.new(0.7, 0, 0.5, 0) 
ToggleButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0) -- Đen tuyền
ToggleButton.BackgroundTransparency = 0.5 -- Trong suốt 50% giống TSB
ToggleButton.Text = "" 
ToggleButton.Active = true
ToggleButton.Draggable = true

-- Bo tròn 100%
local UICorner = Instance.new("UICorner", ToggleButton)
UICorner.CornerRadius = UDim.new(1, 0)

-- [ĐÃ XÓA UISTROKE - KHÔNG CÒN VIỀN NỮA]

-- 3. ICON SHIFT LOCK (Chuẩn TSB)
local LockIcon = Instance.new("ImageLabel", ToggleButton)
LockIcon.Size = UDim2.new(0, 36, 0, 36) -- Nhỏ lại một chút cho cân với nút 79x79
LockIcon.Position = UDim2.new(0.5, -18, 0.5, -18) 
LockIcon.BackgroundTransparency = 1
LockIcon.Image = "rbxasset://textures/ui/mouseLock_off.png" 
LockIcon.ImageColor3 = Color3.fromRGB(255, 255, 255) -- Trắng tinh khiết như icon game

-- 4. HỆ THỐNG GLOW
local EspGlow = Instance.new("Highlight")
EspGlow.Name = "HiruzAimGlow"
EspGlow.FillColor = Color3.fromRGB(255, 50, 50)
EspGlow.FillTransparency = 0.6
EspGlow.OutlineColor = Color3.fromRGB(255, 255, 255)
EspGlow.OutlineTransparency = 0
EspGlow.Enabled = false
EspGlow.Parent = workspace

local function applyGlow(target)
    EspGlow.Adornee = target
    EspGlow.Enabled = true
end

local function removeGlow()
    EspGlow.Enabled = false
    EspGlow.Adornee = nil
end

-- 5. HÀM TÌM MỤC TIÊU 
local function getClosestTarget()
    local closestTarget = nil
    local shortestDistance = math.huge
    local myChar = LocalPlayer.Character
    if not myChar or not myChar:FindFirstChild("HumanoidRootPart") then return nil end

    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("Model") and obj ~= myChar and obj:FindFirstChild(AimPart) then
            local humanoid = obj:FindFirstChildOfClass("Humanoid")
            if humanoid and humanoid.Health > 0 and not obj:IsA("Accessory") then
                if Players:GetPlayerFromCharacter(obj) == LocalPlayer then continue end
                
                local actualDistance = (myChar.HumanoidRootPart.Position - obj[AimPart].Position).Magnitude
                if actualDistance > MaxDistance then continue end

                local screenPos, onScreen = Camera:WorldToViewportPoint(obj[AimPart].Position)
                if onScreen then
                    local screenCenter = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
                    local distanceToCenter = (Vector2.new(screenPos.X, screenPos.Y) - screenCenter).Magnitude
                    
                    if distanceToCenter < shortestDistance then
                        shortestDistance = distanceToCenter
                        closestTarget = obj
                    end
                end
            end
        end
    end
    return closestTarget
end

-- 6. LÕI AIMLOCK (KHÓA SIÊU CỨNG)
RunService:BindToRenderStep("HiruzHardLock", 1000, function()
    if AimlockEnabled and CurrentTarget and CurrentTarget:FindFirstChild(AimPart) then
        local humanoid = CurrentTarget:FindFirstChildOfClass("Humanoid")
        local myChar = LocalPlayer.Character
        
        if humanoid and humanoid.Health > 0 and myChar and myChar:FindFirstChild("HumanoidRootPart") then
            local targetPos = CurrentTarget[AimPart].Position
            local myHRP = myChar.HumanoidRootPart
            
            Camera.CFrame = CFrame.lookAt(Camera.CFrame.Position, targetPos)
            local lookPos = Vector3.new(targetPos.X, myHRP.Position.Y, targetPos.Z)
            myHRP.CFrame = CFrame.lookAt(myHRP.Position, lookPos)
        else
            CurrentTarget = nil
            removeGlow()
            AimlockEnabled = false
            LockIcon.ImageColor3 = Color3.fromRGB(255, 255, 255)
        end
    end
end)

-- 7. XỬ LÝ BẤM NÚT (KHÔNG CHỮ)
ToggleButton.MouseButton1Click:Connect(function()
    AimlockEnabled = not AimlockEnabled
    if AimlockEnabled then
        CurrentTarget = getClosestTarget()
        if CurrentTarget then
            applyGlow(CurrentTarget)
            LockIcon.ImageColor3 = Color3.fromRGB(255, 50, 50) -- Icon đỏ khi trúng
        else
            AimlockEnabled = false
            -- Chớp đỏ tối màu nếu hụt rồi về trắng
            LockIcon.ImageColor3 = Color3.fromRGB(150, 50, 50)
            task.wait(0.2)
            LockIcon.ImageColor3 = Color3.fromRGB(255, 255, 255)
        end
    else
        CurrentTarget = nil
        removeGlow()
        LockIcon.ImageColor3 = Color3.fromRGB(255, 255, 255) -- Về trắng khi tắt
    end
end)
